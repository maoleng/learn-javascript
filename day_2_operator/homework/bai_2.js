//Cho biến sum là tổng của a và b, different là hiệu của a và b. 
//Hãy tìm a và b.

function run(sum, different) {
    var a = (sum + different) / 2
    var b = sum - a


        
    return 'a is ' + a + ' and b is ' + b
}