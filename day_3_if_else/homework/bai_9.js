// Bài tập
// Cho trước 2 biến a và b, nếu 1 trong 2 biến là Number thì gán true cho result.


function hasNumber(a, b) {
    let result = false

    if (a === Number (a) || b === Number(b)){
        result = true;
    }

    return result
}
