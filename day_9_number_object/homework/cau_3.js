// Cho trước biến a và h lần lượt chứa giá trị là kích thước cạnh đáy (a) và chiều cao (h) của tam giác. Hãy tính diện tích tam giác và lưu kết quả vào biến output.

// Ví dụ:

// Input: 100 , 300 - Output: 15000
// Input: 200 , 500 - Output: 50000


function run(a, h) {
    var output = a * h / 2

    return output
}
